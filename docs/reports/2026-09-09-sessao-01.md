# Sessão 01 — 2026-09-09 — Limpeza do repo, rename para Bricklap, base do produto

**Branch**: `chore/monorepo-cleanup` (a partir de `main` em `cc65c7f`). Sem push para `main`.
**Executado por**: Claude Code (Fable 5.1), orientado pelo CTO. **Ambiente**: Windows + WSL 2 (Ubuntu), Node 23.11, npm 10.9 → 11 via `npx`.

## 1. Resumo

O repositório passou de um sandbox Grok Build (auth, base de dados, PWA, ~70 dependências) a um monorepo mínimo com três workspaces: `packages/engine` (motor puro, 104 testes, cobertura 100%), `apps/web-lab` (o lab existente, agora Vite SPA a consumir o motor) e `apps/mobile` (Expo SDK 57, Android only, START/CHANGE/STOP com GPS simulado). Rename Afterlap → Bricklap completo, com migração da chave de persistência. Documentação e ADRs escritos. Todas as verificações da Definition of Done estão verdes. Falta apenas o que exige decisões ou hardware: o APK no telemóvel.

## 2. O que foi feito

| Objetivo | Estado | Commit(s) |
|---|---|---|
| 1. Monorepo `packages/engine`, `apps/web-lab`, `apps/mobile` | Feito | `abd56b0`, `ff7fc9f`, `0d9901c`, `b5e23e7`, `135f870` |
| 2. Rename Afterlap → Bricklap (pacotes, título, chave `bricklap.v1` com migração, docs) | Feito | `f750a70`, `b5e23e7`, docs |
| 3. Remover auth, multiplayer, db, migrations, server, scripts, dependências sem uso | Feito | `2321ec1`, `ff7fc9f` |
| 4. Testes completos do motor | Feito — 104 testes, 100% | "Engine: full test suite (vitest) and seed track fix" |
| 5. Documentação (README, ARCHITECTURE, ROADMAP 0–6, STATUS, ADRs 1–3, BACKLOG, CLAUDE.md, HISTORY) | Feito | "Docs: README, ARCHITECTURE, ROADMAP…" |
| 6. Textos legais intocados, só movidos e com o nome trocado | Feito e verificado por diff | `abd56b0`, `b5e23e7`, `f750a70` |
| Relatório | Este ficheiro | — |

### 2.1 Removido

`src/lib/auth`, `src/lib/multiplayer`, `src/lib/db.ts`, `src/lib/app-data`, `src/lib/env.server.ts`, `src/lib/preview-*`, `src/lib/og`, `src/components/preview-host-bridge.tsx`, `migrations/`, `server/`, `scripts/` (grok-pwa, preview, browser-smoke, check-auth-invariant, migrate, brand-check, sign-out-plan, write-atomic, with-app-env), `startup.sh`, `eslint.config.mjs`, `public/og.jpg`, `AGENTS.project.md`, `GROK.md` (→ `docs/HISTORY.md`), `package-lock.json` antigo.

Dependências removidas: better-auth, jose, kysely, pg, @electric-sql/pglite, @tanstack/react-start, @tanstack/react-query, @tanstack/react-table, nitro, playwright, zod, react-hook-form, @hookform/resolvers, recharts, date-fns, sonner, vaul, cmdk, react-day-picker, react-resizable-panels, tw-animate-css, todos os @radix-ui exceto `react-slot`, eslint e plugins, prettier, globals, lightningcss, @types/pg. Instalação: 571 pacotes (inclui Expo/React Native).

### 2.2 `packages/engine` (`@bricklap/engine`)

- Ficheiros: `src/index.ts` (barrel), `types.ts`, `engine.ts`, `geo.ts`, `seed.ts`; `test/` com 8 ficheiros.
- Alterações à API (todas retrocompatíveis para o lab): `GpsCoords` estrutural em vez de `GeolocationCoordinates` (DOM); `stepSim(..., rng?)`; `createLiveSession(sport, at?, id?)`; `formatClock`/`formatDay` com `locale?`; novos `samplesForSegment` e `recoverLiveSessions`.
- Defeito encontrado pelos testes e corrigido: o `seed.ts` calculava o fim de cada perna a partir do relógio de amostras (que deriva até 8 s por perna), pelo que as últimas amostras da sessão "brick" ficavam 6 s **depois** do evento `stopped`. Agora as pernas são cumulativas desde o início.
- Sem zustand, sem `localStorage`, sem DOM: verificado por grep no fecho.

### 2.3 `apps/web-lab` (`@bricklap/web-lab`)

- Passou de TanStack Start (SSR + nitro) a **Vite SPA** com `@tanstack/react-router` e rotas em ficheiros (`@tanstack/router-plugin`). `index.html` + `src/main.tsx`; `__root.tsx` só hidrata o store e renderiza `<Outlet/>`.
- Componentes e rotas inalterados além dos imports (`@bricklap/engine`, `@/components/bricklap/*`, `@/lib/store`).
- Store: `useBricklap`, `STORAGE_KEY = "bricklap.v1"`, migração única de `afterlap.v1` (adota, grava na chave nova, apaga a antiga; nunca lança).
- Legais: `LEGAL.md`, `src/lib/legal/config.ts`, `src/routes/legal.*.tsx`, `legal-doc.tsx` movidos; diff face a `cc65c7f` contém apenas nome do produto, `storageKey` e caminhos de import.

### 2.4 `apps/mobile` (`@bricklap/mobile`)

- `create-expo-app --template blank-typescript` (SDK 57.0.21, RN 0.86.3, React 19.2.3, TS 6.0.3) + `expo-dev-client`.
- `app.json`: `name` Bricklap, `slug`/`scheme` bricklap, `userInterfaceStyle` dark, `android.package` **placeholder** `com.bricklap.app`, plugin `expo-dev-client`; blocos `ios` e `web` removidos; scripts iOS/web removidos.
- `App.tsx`: um ecrã. Escolher desporto → **Iniciar** → cronómetro, desporto atual, distância total, segmento atual (tempo, distância, ritmo/velocidade), lista de segmentos → **Mudar** (picker inline dos outros desportos) → **Parar** → resumo → **Nova sessão**. Simulador a 1 Hz com `createSim/stepSim/sampleFromSim`; amostra colocada exatamente na fronteira em cada mudança/paragem. Sem persistência, sem GPS real, sem navegação.
- Sem `metro.config.js`: o Expo deteta o monorepo e resolve `@bricklap/engine` (provado pelo `expo export`).

### 2.5 Raiz

`package.json` com workspaces e scripts, `tsconfig.base.json`, `vitest.config.mts`, `.gitignore` (android/ios gerados, dist, coverage, .expo), `.nvmrc` = 24, `engines` node ≥ 22.13 / npm ≥ 11. `LICENSE` e `NOTICE` só com o nome trocado.

## 3. Verificações (evidência)

| Verificação | Resultado |
|---|---|
| `npm test` | 8 ficheiros, **104 testes**, todos verdes, ~0.6 s |
| `npm run test:coverage` | **100%** statements (164/164), branches (85/85), functions (42/42), lines (142/142) em `packages/engine/src` |
| Mutação: limiar 55 → 60 m/s | 1 teste falha (`distance.test.ts`) |
| Mutação: filtro desligado (55 → ∞) | 3 testes falham |
| Mutação: janela `recovered` 2000 → 1000 ms | 1 teste falha |
| Mutação: `samplesInRange` com fim exclusivo | 9 testes falham |
| Seed original (pré-correção) | 1 teste falha ("amostras dentro de [start, stop]") |
| `npm run typecheck` | Limpo nos três workspaces (TS 6.0.3) |
| `npm run build:web` | OK, 19 chunks, JS principal 282 kB (92 kB gzip) |
| Dev server do lab | `/` e `/legal/privacidade` → 200, `<title>Bricklap</title>`, `/src/main.tsx` servido |
| `expo export --platform android` | OK, 585 módulos, bundle Hermes 1.5 MB |
| `expo config --type public` | Sem chaves `ios`/`web`; `android.package` presente |
| grep `afterlap` | Só `LEGACY_STORAGE_KEY` (migração), o README do lab que a documenta e `docs/HISTORY.md` |
| grep DOM/RN/zustand em `packages/engine/src` | Vazio |

Não verificado: a app **num telemóvel** (não há Android SDK/JDK nem `adb` na WSL, e não há conta EAS configurada). Ver §9.

## 4. O que ficou por fazer

- APK de desenvolvimento e execução no telemóvel do CTO (§9).
- Conta/projeto EAS e `android.package` definitivo (decisões).
- Ícones e splash reais (assets do template).
- Merge em `main` (decisão do CTO após revisão do branch).
- Não instalei Node 24 nem npm 11 globalmente (alteração ao ambiente, não ao repo). `.nvmrc` e README dizem como.

## 5. Decisões tomadas e porquê

| Decisão | Porquê | Alternativa rejeitada |
|---|---|---|
| **npm workspaces** | Já existe na máquina, zero dependências extra, chega para três workspaces. | pnpm/turbo/nx: mais ferramentas antes de haver problema de escala. |
| **Motor consumido como TypeScript de origem** (`exports` → `src/index.ts`) | Vite e Metro transpilam pacotes do workspace; evita build e `dist/` desatualizado. | tsup/tsc: só quando o motor sair do repo. |
| **Lab passa de TanStack Start a Vite SPA** | O lab é 100% cliente (localStorage, GPS simulado). SSR/nitro/Vercel eram exigências do sandbox, não do produto. Menos 4 dependências pesadas e um servidor; componentes e rotas iguais. | Manter Start: mais dependências, build preso a um preset de alojamento. |
| **Expo SDK 57 + expo-dev-client** | Estável mais recente (RN 0.86, React 19.2, TS 6). Dev client permite módulos nativos (GPS em segundo plano na Fase 2/3). | Expo Go (bloqueia nativos); SDK 54 (dois lançamentos atrás). |
| **Uma versão de React, @types/react e TypeScript** em todo o workspace | Metro rebenta com dois Reacts; hoisting limpo. | — |
| **vitest 4** | vitest 5 exige Node ^22.12/^24; o 4 aceita mais. Ambos avisam com Node 23 mas funcionam. | `node:test`: sem cobertura integrada, imports sem extensão não resolvem. |
| **Lockfile gerado com npm 11** | npm 10.9.2 falha com `edgesOut` (bug do arborist com peers opcionais do vitest em workspaces). | `--legacy-peer-deps`: mascara e desliga a verificação de peers. |
| **`.nvmrc` = 24**, `engines` ≥ 22.13 | Node 23 está fora do suporte de react-native 0.86 e vitest. Não alterei o Node da máquina. | — |
| **LICENSE e NOTICE ficam na raiz; `LEGAL.md` vai para `apps/web-lab`** | LICENSE diz "este repositório" e o GitHub lê-o na raiz; NOTICE é referenciado pelo LICENSE. `LEGAL.md` descreve o estado legal do lab web. | Mover os três (leitura literal da instrução) deixaria o repo sem licença visível. **A confirmar (§6).** |
| **Textos legais intocados**, incluindo o caminho antigo `src/lib/legal/config.ts` | Instrução explícita: só mover e trocar o nome. | Corrigir o caminho (uma linha em 3 ficheiros) — ver §6. |
| **Migração `afterlap.v1` → `bricklap.v1`** única, com remoção da chave antiga | Trivial e não perde histórico de quem usou o lab. | Ler as duas chaves para sempre. |
| **Motor sem plataforma**: `GpsCoords` estrutural, `rng` injetável, `at` por parâmetro | Exigência do CTO + testes determinísticos; `GeolocationCoordinates` não existe em React Native. | — |
| **`samplesForSegment` inclusivo nos dois limites** | A amostra de fronteira é o último ponto do segmento anterior e o primeiro do seguinte; a soma das distâncias dos segmentos iguala a da sessão (testado). Comportamento original mantido, agora nomeado. | Intervalos semi-abertos: buraco de distância em cada mudança. |
| **`android/` e `ios/` não versionados** (CNG) | Configuração em `app.json`; nativo regenera-se com `expo prebuild`. | Versionar nativo: conflitos sem ganho na Fase 1. |
| **ESLint/Prettier fora** | "Sem dependências além de Expo + testes". `.prettierrc` fica como convenção de editor. | — |
| **Sem iOS** | Fase 1 Android only. | — |
| **Correção do seed** | Amostras depois do `stopped` são um defeito real; a correção é local e testada. | Deixar e relaxar o teste. |

## 6. Dúvidas para o CTO

1. **LICENSE/NOTICE na raiz ou em `apps/web-lab`?** Deixei na raiz (§5). Se preferires a leitura literal, é um `git mv`.
2. **Caminho antigo nos textos legais.** `legal.index.tsx`, `LEGAL.md` e `LICENSE` referem `src/lib/legal/config.ts`, que agora é `apps/web-lab/src/lib/legal/config.ts`. Posso atualizar? (Não é redação legal, é um caminho de ficheiro, mas a regra era "não tocar".)
3. **`android.package`**: `com.bricklap.app` é placeholder. Qual o domínio (ex.: `pt.bricklap.app`)? Mudar depois de publicar é doloroso.
4. **Conta EAS**: quem é o dono da conta Expo/EAS? Sem ela, a Opção A do §9 não é possível.
5. **`NOTICE`** não lista Expo/React Native. Atualizar agora ou só quando a app for distribuída?
6. **Cópia da app Android em pt-PT** (Iniciar/Mudar/Parar) enquanto o lab e `SPORT_META` estão em inglês. Manter assim na Fase 1, ou decidir i18n já?
7. **Vite SPA em vez de TanStack Start**: aceitas a troca (§5)? Reverter é possível mas traz nitro/SSR de volta.
8. **Node 24 / npm 11 na máquina**: queres que instale via nvm numa próxima sessão?

## 7. Próximos passos sugeridos

1. Rever o branch e fazer merge em `main` (ou pedir ajustes).
2. Responder às dúvidas 3 e 4; criar o projeto EAS; produzir o primeiro APK (Opção A do §9) e validar START/CHANGE/STOP no telemóvel.
3. Instalar Node 24 LTS e npm 11 na WSL.
4. Sessão 02 (Fase 2): `expo-location` em primeiro plano, persistência local na app, `recovered` real, ícones.
5. CI mínima (GitHub Actions) com os quatro comandos da Definition of Done.

## 8. Comandos

Instalar (uma vez, a partir da raiz):

```bash
npx -y npm@11 install
```

Testes do motor e cobertura:

```bash
npm test
```

```bash
npm run test:coverage
```

Typecheck de tudo:

```bash
npm run typecheck
```

Lab web (http://localhost:8080) e build:

```bash
npm run dev:web
```

```bash
npm run build:web
```

Prova do bundle Android sem SDK (Metro end-to-end):

```bash
npm run export:android -w @bricklap/mobile
```

Metro para um dev client já instalado no telemóvel:

```bash
npm run dev:mobile
```

## 9. Correr a app no telemóvel Android a partir de WSL 2

Contexto: a app usa **expo-dev-client**. O telemóvel precisa de um APK de desenvolvimento construído a partir do projeto (o Expo Go não serve). Depois do APK instalado, as alterações a TypeScript chegam por Metro (`npm run dev:mobile`), sem reconstruir o nativo.

### Opção A — EAS Build (nuvem da Expo)

A partir de `apps/mobile`:

```bash
npx eas-cli@latest login
```

```bash
npx eas-cli@latest build:configure
```

```bash
npx eas-cli@latest build --platform android --profile development
```

O `eas.json` gerado deve ter o perfil `development` com `developmentClient: true`, `distribution: "internal"` e `android.buildType: "apk"`. No fim a Expo dá um link/QR; abre-se no telemóvel e instala-se o APK. Depois `npm run dev:mobile` na WSL e ler o QR na app (telemóvel e PC na mesma rede Wi-Fi; se a rede bloquear, `npx expo start --dev-client --tunnel`).

Prós:
- Zero instalação de Android SDK/JDK na WSL; funciona com o ambiente atual.
- Keystore gerido pela Expo.
- Build reprodutível na nuvem, reutilizável em CI e para partilhar APKs.
- Não depende de USB nem de usbipd.

Contras:
- Exige conta Expo e projeto EAS (`projectId` em `app.json`) — decisão do CTO.
- Fila e tempo de build na nuvem (minutos); o plano gratuito tem limite mensal e prioridade baixa.
- O código sobe para servidores da Expo (aceitável num protótipo; um terceiro a considerar quando houver dados reais).
- Cada alteração nativa (módulo novo, permissões) obriga a novo build na nuvem.

### Opção B — Build local na WSL 2 (Android SDK + adb)

Instalar na WSL: JDK 17 (`sudo apt install openjdk-17-jdk`), Android command-line tools + `sdkmanager "platform-tools" "platforms;android-3x" "build-tools;3x.0.0"` (versões exatas: as que o `expo prebuild` do SDK 57 pedir), `ANDROID_HOME` e `JAVA_HOME`. Depois, em `apps/mobile`:

```bash
npx expo run:android --device
```

Gera `android/` (ignorado pelo git), compila com Gradle e instala via `adb`. Para o `adb` da WSL ver o telemóvel:

1. **adb por Wi-Fi (recomendado, sem USB passthrough)** — Android 11+: Definições → Opções de programador → Depuração sem fios → Emparelhar com código. Na WSL:

```bash
adb pair IP:PORTA_DE_EMPARELHAMENTO
```

```bash
adb connect IP:PORTA
```

2. **USB via usbipd-win** — no Windows (PowerShell admin): `winget install --interactive --exact dorssel.usbipd-win`; depois `usbipd list`, `usbipd bind --busid <id>` e `usbipd attach --wsl --busid <id>`. Na WSL: `lsusb`, `adb devices`. Pode exigir regras udev. Enquanto anexado, o Windows não vê o telemóvel.

Prós:
- Ciclo rápido depois da primeira instalação (rebuild nativo em 1–3 min, sem fila).
- Sem conta nem custos externos; o código não sai da máquina.
- Necessário mais tarde de qualquer forma (logcat, perfis, módulos nativos).

Contras:
- Instalação pesada (SDK + JDK + Gradle: vários GB; primeiro build lento).
- WSL 2 não tem USB nativo; usbipd funciona mas é frágil (desanexa ao suspender, `bind` precisa de admin). O adb por Wi-Fi evita isto.
- Emulador Android dentro da WSL não é prático; se quiser emulador, corre-o no Windows e liga o adb da WSL ao do Windows.
- Keystore de debug local; para release é preciso keystore próprio.

### Recomendação

Para validar a Fase 1 no telemóvel esta semana: **Opção A (EAS Build) para o primeiro APK** — menos atrito a partir da WSL. Preparar em paralelo a **Opção B com adb por Wi-Fi** para a Fase 2 (GPS real, foreground service), quando o ciclo nativo local compensa.

Alternativa sem nuvem: build no Windows (Android Studio) com o repo em `\\wsl$` é possível mas lento (I/O entre sistemas de ficheiros); não recomendado.

## 10. Incidente de execução

A orquestração multi-agente desta sessão (implementação → revisão adversarial → correção → documentação → verificação) foi lançada, mas os 11 agentes falharam com o **limite de sessão da conta** ("session limit, resets 18:50") após ~4 minutos. Dois agentes já tinham escrito a maior parte do port do web-lab e do esqueleto mobile; esse trabalho foi revisto linha a linha, corrigido onde necessário e verificado (typecheck, build, smoke test, export). Os testes do motor, a documentação e este relatório foram feitos diretamente. A revisão adversarial por agentes foi refeita depois do reset do limite, em escala reduzida; o resultado está no §11.

## 11. Revisão adversarial (pós-reset)

_(preenchido no fim da sessão)_
