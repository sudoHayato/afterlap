# Sessão 05 — Desportos sem GPS (Fase 3, parte 1)

**Data**: 2026-09-10 · **Branch**: `feat/desportos-indoor` (a partir de `main` em `fafe295`)
**Executado por**: Claude Code (Fable 5.1), orientado pelo CTO. **Ambiente**: Windows 10 + WSL 2, Node 24.21.0 / npm 11.19.0, JDK 17 + SDK 36 locais, telemóvel Galaxy S24 Ultra por USB (`adb.exe` do Windows).

## 1. Resumo

A app grava treinos de ginásio e piscina: força, remo indoor, passadeira e natação são desportos só de tempo, sem GPS. Uma sessão pode misturar segmentos com e sem GPS (força → corrida na rua → força), e o motor garante que um segmento sem GPS não tem amostras, distância nem ritmo — nem sequer os que a interpolação de fronteira inventaria entre a última posição antes do ginásio e a primeira depois. O watcher de posição liga-se e desliga-se com o segmento; a permissão de localização só se pede na primeira vez que faz falta. Oito chips no ecrã inicial, em duas linhas, um toque para começar como antes.

Persistência, textos legais, filtro de ritmo e segundo plano não mudaram. Decisões em [ADR 0008](../adr/0008-desportos-sem-gps.md).

## 2. Feito

| # | Entregável | Estado | Onde |
|---|---|---|---|
| 1 | Motor: desportos com `hasGps` e `paceKind: none`; segmentos sem amostras; testes para sessões mistas e para a fronteira com/sem GPS | feito | `packages/engine/src/types.ts` (`SPORT_HAS_GPS`, `sportHasGps`), `engine.ts` (`gpsSpan`, `samplesForSegment`, `segmentMetrics`, `sessionMetrics`), `test/indoor.test.ts` (18 testes), `test/geo.test.ts` |
| 2 | App: watcher ligado ao ciclo de vida do segmento; permissão sob demanda; ecrã de gravação e resumo adaptados; oito chips em duas linhas | feito | `apps/mobile/App.tsx` |
| 3 | Testes no dispositivo a passar com o build novo | **por fazer — telemóvel não ligado** (§4.2) | `apps/mobile/device/recovery.device.test.ts` |
| 4 | Build de release instalado e a arrancar sem Metro; APK de debug guardado no PC | **build feito e no PC; instalação por fazer — telemóvel não ligado** (§4.3) | `C:\Users\<utilizador>\bricklap-release.apk`, `bricklap-debug.apk` |
| 5 | Relatório com dúvidas para o CTO; push | feito | este ficheiro |
| — | Nomes em en + pt-PT via `t()` | feito | `packages/i18n` (`sport.*`, `mobile.sportGroup*`, `mobile.gpsNoPermission`, `mobile.gpsServicesOff`) |
| — | Ícones dos quatro desportos no lab web | feito | `apps/web-lab/src/components/bricklap/icons.tsx` |
| — | ADR, ARCHITECTURE, READMEs, ROADMAP, BACKLOG, STATUS | feito | `docs/adr/0008-desportos-sem-gps.md` e restantes |

### 2.1 Desenho

- **`SPORT_HAS_GPS` no motor**, ao lado de `SPORT_PACE_KIND`: lógica de domínio, não rótulo. `sportHasGps(sport)` é a única pergunta que a app faz.
- **`gpsSpan`**: para um segmento com GPS, as amostras candidatas à interpolação de fronteira ficam limitadas ao troço contíguo de segmentos com GPS a que ele pertence. A cerca só existe onde o vizinho é um segmento sem GPS; para o resto o intervalo fica aberto, para que um segmento ao vivo continue a interpolar até `at` (o teste existente "the open segment of a live session ends at `at`, interpolating there if needed" apanhou a primeira versão, que fechava o intervalo em `at` e deixava de interpolar).
- **`sessionMetrics` = soma dos segmentos.** Antes era um cálculo à parte sobre todas as amostras da sessão; com um ginásio entre duas corridas, isso contava o deslocamento entre elas. Para sessões só de rua o resultado é idêntico — a interpolação partilhada nas fronteiras já garantia a soma (é o que o teste das *seeds* verifica). Os 176 testes anteriores passam sem alteração de expectativas.
- **Dois efeitos em vez de um** na app: o relógio corre enquanto há gravação; o feed (simulador ou subscrição real) corre enquanto `feedWanted` — sessão ao vivo **e** desporto atual com GPS. Um CHANGE muda `feedWanted` e o efeito desmonta ou monta a subscrição. A permissão é pedida dentro do efeito, antes de subscrever: já concedida, resolve sem diálogo; recusada, a linha do GPS mostra "GPS indisponível · sem permissão de localização" e o segmento continua a contar tempo.
- **`start()`** pede permissão só se o desporto inicial tiver GPS (uma recusa fica tratada no ecrã inicial, como antes); **`boundarySample()`** devolve `null` se o segmento que acaba não tiver GPS.
- **Ecrã**: `LiveScreen` esconde distância total, linha do GPS, distância e ritmo do segmento quando o desporto atual não tem GPS; `SegmentList` mostra só o tempo nesses segmentos; `SummaryScreen` mostra a distância só quando há pelo menos um segmento com GPS.
- **Chips**: `SportPicker` agrupa por `sportHasGps` — "Rua" e "Ginásio / Piscina" — em duas linhas com rótulo, mantendo os `testID`s por desporto.

## 3. Decisões

| Decisão | Porquê | Reversível? |
|---|---|---|
| Distância da sessão como soma dos segmentos | Única forma de o total obedecer às regras dos segmentos sem duplicar a cerca no total | Sim |
| Cerca de interpolação só junto de segmentos sem GPS (`gpsSpan` aberto nos outros lados) | Fechar o intervalo em `at` quebrava a interpolação do segmento ao vivo — apanhado por um teste existente | — |
| Permissão pedida dentro do efeito do feed, e também antes de iniciar quando o desporto inicial tem GPS | Cobre "força → corrida" (pede a meio) e "corrida" (pede antes, recusa tratada no ecrã inicial) com o mesmo código | Sim |
| Esconder a distância total durante um segmento sem GPS, mesmo numa sessão mista | À letra do brief ("só o cronómetro do segmento e o total") | Sim — ver §5 |
| Resumo sem a métrica "Distância" quando a sessão não tem segmentos com GPS | "0 m" num circuito de ginásio é ruído | Sim |
| `SIM_SPEED_MPS = 0` para os desportos sem GPS | O simulador não deve mover-se num segmento de ginásio; o teste garante-o | — |
| `nextSport` percorre os oito | Só o mostrador de relógio do lab o usa; agrupar ali seria trabalho a mais para um lab | Sim |
| Sem dependências novas | Nada de nativo mudou; o `android/` gerado serviu como estava | — |

## 4. Validação

### 4.1 Estática (DoD)

`npm test` **197/197** (era 176; +18 em `indoor.test.ts`, +2 em `geo.test.ts`, +1 em `dictionary.test.ts`), cobertura do motor e do i18n a **100 %** em declarações, ramos, funções e linhas (confirmado com `json-summary`, porque o reporter de texto esconde ficheiros a 100 %). `npm run typecheck` limpo nos quatro workspaces; `npm run build:web` ok; `npm run export:android` ok (659 módulos). Motor, persistência e textos legais: só o motor mudou, dentro do que o brief pede; `apps/mobile/persistence/` e os caminhos legais têm `git diff` vazio. `assembleRelease` e `assembleDebug` limpos; o bundle de release contém os desportos novos (verificado no APK).

### 4.2 Teste de recuperação no dispositivo — por fazer

**O telemóvel não esteve ligado por USB durante a sessão.** `adb devices` vazio do princípio ao fim, e o Windows não lista nenhum dispositivo ADB/Samsung entre os presentes (só a TV e o disco); reiniciar o servidor `adb` não mudou nada; um vigia esperou ~10 min pela porta. Nada do que segue foi corrido. O teste não devia mudar de resultado: continua a tocar em "Simulado" e "Iniciar" com o desporto por defeito (corrida), e nenhum `testID`/`accessibilityLabel` mudou — mas "devia" não é "passou".

Para correr, com o telemóvel ligado (o Metro e o relé IPv4 ficaram a correr nesta máquina):

```sh
adb.exe reverse tcp:8081 tcp:8082          # Metro da WSL só se expõe em [::1]:8081; o relé faz IPv4
adb.exe shell pm install -r /data/local/tmp/bricklap-debug.apk   # ou push do APK de debug novo primeiro
npm run test:device -w @bricklap/mobile
```

### 4.3 Build de release no telemóvel — por fazer

`assembleRelease` limpo (53 s), 76 MB, bundle JS embutido com os desportos novos (verificado dentro do APK: `rowing_indoor`, `swimming_pool`, "Remo indoor", os rótulos de grupo). Copiado para `C:\Users\<utilizador>\bricklap-release.apk`; o de debug (`assembleDebug`, também limpo) para `C:\Users\<utilizador>\bricklap-debug.apk`, como antes. **Não instalado**, pela mesma razão. O critério "app de release instalada e a arrancar sem Metro" da DoD desta sessão **não está cumprido**; o fundador precisa dele para correr amanhã.

Procedimento, com o telemóvel ligado (5 min):

```sh
adb.exe push "C:\Users\<utilizador>\bricklap-release.apk" /data/local/tmp/bricklap-release.apk
adb.exe shell pm install -r /data/local/tmp/bricklap-release.apk   # "Não enviar" no diálogo do Play Protect; os dados mantêm-se (mesma chave)
adb.exe shell am force-stop com.bricklap.app
adb.exe shell am start -W -n com.bricklap.app/.MainActivity        # sem Metro: Status ok, LaunchState COLD
adb.exe shell uiautomator dump /sdcard/ui.xml && adb.exe shell cat /sdcard/ui.xml | grep -o 'text="[^"]*"' | head -30
```

O ecrã inicial tem de mostrar "RUA" com Corrida, Bicicleta, Caminhada, Transição e "GINÁSIO / PISCINA" com Força, Remo indoor, Passadeira, Natação (piscina). Prova rápida do ADR 0008 no aparelho, sem sair de casa: `pm revoke com.bricklap.app android.permission.ACCESS_FINE_LOCATION`, tocar em **Força** → **Iniciar**: nenhum diálogo de permissão, o cronómetro anda, não há linha de coordenadas; **Mudar** → **Corrida**: o diálogo aparece aí; **Mudar** → **Força**: a linha do GPS desaparece e o `dumpsys location` deixa de listar o pedido da app. No fim, `pm grant` da permissão para o fundador não a ter de dar outra vez.

## 5. Dúvidas para o CTO

- **Distância total escondida durante um segmento sem GPS.** Segui o brief à letra: num segmento de força a app mostra o cronómetro do segmento e o total, e mais nada — mesmo que a sessão já tenha uma corrida com 5 km atrás. A alternativa, uma linha, é mostrar a distância total sempre que a sessão tenha pelo menos um segmento com GPS (como o resumo já faz). Prefiro a segunda para sessões mistas; fica como está até dizeres.
- **"Apenas desta vez" numa sessão força → corrida.** Se o fundador escolher essa opção quando a permissão é pedida a meio, um `force-stop` + retomar volta a pedir — o efeito do feed pede outra vez e trata a recusa na linha do GPS. Comportamento correto, mas o diálogo aparece a meio do treino; documentado, não contornado.
- **O simulador do lab web continua a gerar amostras num segmento de ginásio.** O motor ignora-as (distância 0), por isso o lab mostra o comportamento certo, mas as amostras existem. Não vale a pena mexer no lab por isto; registo só para não parecer esquecimento.
- **`nextSport` no mostrador do relógio do lab** percorre os oito desportos, ginásio incluído. Se o relógio (Fase 5) só fizer sentido na rua, filtra-se ali.
- **Introdução manual de distância** (metros de remo, piscinas) ficou no BACKLOG como fase futura, sem desenho.

## 6. Estado do telemóvel e da máquina

- **Telemóvel**: não esteve ligado. Continua com o que a sessão 04 deixou: APK de **debug** da sessão 04 instalado (precisa do Metro), permissão de localização concedida, os dois APKs antigos em `/data/local/tmp/`.
- **Máquina**: o **Metro** (`expo start --dev-client --port 8081 --clear`) e o **relé IPv4** (`0.0.0.0:8082 → 127.0.0.1:8081`) ficaram a correr, de propósito, para o teste de dispositivo e o dev client arrancarem à primeira quando o telemóvel for ligado. Os APKs novos estão em `C:\Users\<utilizador>\bricklap-{debug,release}.apk` e em `apps/mobile/android/app/build/outputs/apk/{debug,release}/`.

## 7. Por fazer / próximos passos

- **Primeiro, com o telemóvel ligado (≈ 10 min)**: teste de recuperação no dispositivo (§4.2) e instalação do release + arranque sem Metro + prova do fluxo força → corrida → força (§4.3). É o que falta para a DoD desta sessão e para o fundador correr amanhã sem PC.

- **Filtro de ritmo** (sessão seguinte da Fase 3): o ritmo em caminhada que o fundador achou mal calibrado (sessão 04 §6.1). Não tocado aqui.
- **Segundo plano** (Fase 3): foreground service, ecrã desligado. Não tocado aqui.
- **Introdução manual** de distância nos desportos sem GPS: fase futura.
- **Exportação de dentro da app** (BACKLOG, herdado da sessão 04).
